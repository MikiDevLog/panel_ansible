all:
  hosts:
    panel-host:
      ansible_host: "{{ ansible_host }}"
      ansible_user: "{{ ansible_user }}"
      ansible_ssh_private_key_file: "{{ ansible_ssh_private_key_file }}"
      panel_docker_dir: "{{ panel_docker_dir }}"
  vars:
    ansible_python_interpreter: /usr/bin/python3
